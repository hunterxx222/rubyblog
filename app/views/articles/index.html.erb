<div class="container">
  <p id="notice"><%= notice %></p>

<nav class="navbar navbar-light bg-faded">
        <button class="navbar-toggler hidden-sm-up" type="button" data-toggle="collapse" data-target="#navbar-header" aria-controls="navbar-header">
          &#9776;
        </button>
        <div class="collapse navbar-toggleable-xs" id="navbar-header">
          <a class="navbar-brand" href="/">Toan's Blog</a>
          <ul class="nav navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" target="_blank" href="walkthrough.gif">Walkthrough</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">About</a>
            </li>
          </ul>
          	<form class="form-inline pull-xs-right">
				<%= form_tag(articles_path(@articles), :method => "get") do %>
				<%= text_field_tag :search, params[:search], placeholder: "Search Articles", class: 'form-control'%>
				<%= submit_tag "Search", class: "btn btn-outline-success"%>
			</form>
        </div>
      </nav> 
      <!-- /navbar -->
<h3>My first rails application for CoderSchool :-)</h3>
<% end %>
<% if @articles.present? %>
  <%= render @articles %>
<% else %>
  <p>Unfortunately there are no articles containing the word(s) <%= params[:search] %>.</p>
<% end %>
<!--End Search Form -->

<br>

<%= link_to 'New Article', new_article_path %>

</div>

